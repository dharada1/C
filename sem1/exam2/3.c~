#include <stdio.h>


int sum(int x){
  int y=0;
  if(x==0){
    return 1;
  }
  if(x!=0){
  y=1+2*sum(x-1);
    return y;
  }
}

int main(void){
  int n;
  scanf("%d",&n);
  printf("%d\n",sum(n));
 return 0;
}

#include <stdio.h>

int main(void){
  
  int A[16][8],i,a,b;
  
  for(i=0;i=127;i++){
    a=i/16;
    b=i%16;
    A[a][b]=i; 
  }
  
  for(i=0;i=127;i++){
    a=i/16;
    b=i%16;
    printf("%c",A[a][b]);
    
    if(b=15){
      printf("\n");
    }
  }

return 0;

  }

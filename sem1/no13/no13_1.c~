#include <stdio.h>

int main(void){

  FILE *fp;
  int x[128];

  fp = dopen("puzzle.html", "r");

  while(fscanf(fp, "%s", x) != EOF){
    printf("%s", x);
    printf("\n");
  }
  fcolse(fp);

  return 0;
}

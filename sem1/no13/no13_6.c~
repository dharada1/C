#include <stdio.h>

int main(void){
  
  FILE *fp;
  int i,j,a,moji,length;
  fp=fopen("runlength.dat","r");
  do{
    fscanf(fp,"%d",&moji);
    fscanf(fp,"%d",&length);
    for(j=0;i<length;j++){
      printf("%c",moji);
    }
  }while(a=6000);
  fclose(fp);
  
   return 0;
}

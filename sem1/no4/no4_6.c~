#include <stdio.h>

int main(void)
{
  int a;
  scanf("%d",&a);
  
  switch (){
  case 1 :;
  case 2 :;
}
  printf("%d\n",b);

子（シ）、丑（チュウ）、寅（イン）、卯（ボウ）、
辰（シン）、巳（シ）、午（ゴ）、未（ビ）、
申（シン）、酉（ユウ）、戌（ジュツ）、亥（ガイ）
  return 0;
}

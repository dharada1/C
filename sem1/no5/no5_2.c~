#include <stdio.h>
#include <stdlib.h>

int main(void)
{
  int i,a,max,min;
  a=rand() % (100+1);
  max = a;
  min = a;

  for (i=1;i<=4;i++) {
    a=rand() % (100+1);
    if (a>=max){
      max = a;
    }
    else if (min>=a){
      min = a;
    } 
  }

  printf("%d\n%d", max,min);
  return 0;
}

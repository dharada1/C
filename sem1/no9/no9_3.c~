#include <stdio.h>

int factorial(int x){
  if (x == 1){
    return 1;
  }
  else{
    return factorial(x - 1)*x;
  }
}

int main(void){
  int n;
  scanf("%d",&n);
  printf("%d\n",factorial(n));
  return 0;
}

#include <stdio.h>
#include <math.h>
#define DEGREES * (3.1415926/180.0)

int main(void)
{
  int *xtr = malloc( sizeof(int));
 int *ytr = malloc( sizeof(int));


 if (xtr,ytr == NULL)
    {
      fprintf(stderr, "memory allocation fault.\n");
      exit(1);
    }

  scanf ("%d", ptr);
  scanf("%d",ytr);
















  return 0;
}
